/*
 * 云端账户配置
 * 1. 在 Supabase 创建项目。
 * 2. 执行 supabase-schema.sql。
 * 3. 将 Project URL 和 publishable/anon key 填到下方。
 *
 * 注意：浏览器端只能使用 publishable/anon key，绝对不要放 service_role key。
 */
window.ENERGY_LAB_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
};
